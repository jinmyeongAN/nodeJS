mixin show(info)
    div
        h4
            a(href=`videos/${info.id}`)=info.title
        ul
            li #{info.views} #{info.views === 1 ? "view" : "views"}.
            li The rating is #{info.rating}.